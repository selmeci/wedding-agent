/**
 * Report Agent State
 *
 * Minimal state - only message history is persisted
 * No need for complex state like in Chat agent
 */

// biome-ignore lint/complexity/noBannedTypes: valid
export type ReportAgentState = {};
